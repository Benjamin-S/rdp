# rdp
Random Date Picker

Tool created to select a number of dates from a date range for random queries. Includes the option to filter out weekends, US public holidays and UK public holidays. Dates can be output in CSV format.
